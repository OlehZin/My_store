h1 Create new product
| <%= simple_form_for @product do |f| %
| <%= f.label "Зображення:" %
| <%= f.file_field :image %

| <%= render partial: "form", locals: {f: f}%
p <%= f.submit "Зберегти товар" %
| <% end %
<h1>Create new product</h1>
  
  <%= form_for @product do |f| %>
  <%= f.label "Зображення:" %>
  <%= f.file_field :image %>
  
  <%= render partial: "form", locals: {f: f}%>
  
  <p><%= f.submit "Зберегти товар" %></p>
  <% end %>